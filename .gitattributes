# Normalize line endings to LF in the repo (consistent across Windows/macOS/Linux and CI).
* text=auto eol=lf

# Binary assets — never normalize.
*.ico binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.woff binary
*.woff2 binary
